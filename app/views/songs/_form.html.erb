<%= form_for @song  do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= artist_select(@song.artist, @song) %>
  <%= artist_se_field(@song) %>
  <br />

  <%= f.submit %>
<% end %>
